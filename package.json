{
  "name": "simple-web-proxy",
  "version": "1.1.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "node-fetch": "^3.2.0",      // 追加
    "fetch-cookie": "^1.0.0",    // 追加
    "cors": "^2.8.5"              // 追加
  },
  "engines": {
    "node": ">=18"
  }
}
